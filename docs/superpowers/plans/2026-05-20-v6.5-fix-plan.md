# v6.5 Fix Plan — Critical Bug + Documentation Gaps

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development or executing-plans to implement task-by-task.

**Goal:** Исправить critical-баг (NameError в enforcement gates) и 4 документационных несоответствия, выявленных аудитом pipeline-refactor-plan.

**Architecture:** Блок enforcement gates в `_run_pipeline_steps()` использует переменные `stone_gamma` и `compression` ДО их определения (строки 654-698 vs 726-728). Docs содержат устаревшие/неверные значения и имена. Каждый фикс независим и тестируется отдельно.

**Tech Stack:** Python 3.14, retouch/processing/pipeline.py, docs/

**Prep — проверить актуальные строки перед началом:**
```bash
cd G:\Dev\Projects\GRANITE\granite-retouch
```

---

### Task A: Critical Bug — enforcement gates используют переменные до определения

**Problem:** В `retouch/processing/pipeline.py` блок enforcement (строки ~654-698) обращается к `stone_gamma` и `compression`, которые присваиваются только в строках ~726-728. При срабатывании `variance_loss`, `clipped_pct` или `p95_shift` → NameError.

**Solution:** Сдвинуть присвоение `stone_gamma` и `compression` ДО блока enforcement.

**Files:**
- Modify: `retouch/processing/pipeline.py`

- [ ] **Step 1: Найти точные номера строк**

Run:
```powershell
cd G:\Dev\Projects\GRANITE\granite-retouch
Select-String -Path retouch/processing/pipeline.py -Pattern "(stone_gamma|compression).*machine_cfg\.get|triggered.*gate" | Select-Object -First 20
```

Note actual line numbers (may differ from plan).

- [ ] **Step 2: Найти блок Gates enforcement**

Run:
```powershell
Select-String -Path retouch/processing/pipeline.py -Pattern "Gates enforcement" | ForEach-Object { $_.LineNumber }
```

Expected: ~2 lines (variance_loss, clipped_pct comments).

- [ ] **Step 3: Найти присвоение stone_gamma и compression**

Run:
```powershell
Select-String -Path retouch/processing/pipeline.py -Pattern "^(\s*stone_gamma\s*=|^\s*compression\s*=)" | ForEach-Object { "$($_.LineNumber): $($_.Line.Trim())" }
```

- [ ] **Step 4: Перенести присвоение ДО блока enforcement**

Проверить реальные номера строк. Если `stone_gamma` и `compression` определяются в строках ~726-728, а блок enforcement в ~652-698, то нужно поднять определения перед блоком enforcement.

```python
# Read the exact context first
```

Read file around line 650-730, then:

```python
# Cut the assignments from their current location and paste them before
# the enforcement block, something like:
stone_gamma = ctx.machine_cfg.get("stone_gamma", None)
compression = ctx.machine_cfg.get("rolloff_compression", 0.35)

# (then the existing enforcement block follows)
```

Remove the duplicated assignments from later in the function.

- [ ] **Step 5: Проверить что нет NameError**

Run:
```powershell
& ".venv\Scripts\python.exe" -c "from retouch.processing.pipeline import _run_pipeline_steps; print('import ok')"
```

Expected: "import ok"

- [ ] **Step 6: Запустить связанные тесты**

```powershell
& ".venv\Scripts\python.exe" -m pytest tests/test_quality_gates.py tests/test_pipeline_with_gates.py -v --no-header
```

Expected: 20+3 = 23 passed

- [ ] **Step 7: Commit**

```powershell
git add retouch/processing/pipeline.py
git commit -m "fix: move stone_gamma/compression assignments before enforcement block

Blocks enforcement (variance_loss, clipped_pct, p95_shift) accessed 
stone_gamma and compression before they were defined, causing NameError.
Assignments moved before the enforcement block.
Fixes v6.5 audit critical bug."
```

---

### Task B: Wire unused gates — pre_check_skin_delta_envelope + post_check_shadow_crush

**Problem:** `pre_check_skin_delta_envelope` и `post_check_shadow_crush` импортированы в pipeline.py, но нигде не вызываются в `_run_pipeline_steps()`.

**Files:**
- Modify: `retouch/processing/pipeline.py`

- [ ] **Step 1: Найти где вызываются остальные gates**

```powershell
Select-String -Path retouch/processing/pipeline.py -Pattern "pre_check_|post_check_" | ForEach-Object { "$($_.LineNumber): $($_.Line.Trim())" }
```

- [ ] **Step 2: Добавить вызов pre_check_skin_delta_envelope**

После pre-checks (около строки 545, рядом с другими вызовами pre_check_*):

```python
    # Gate: skin_delta_envelope
    if validated.plan.skin_delta is not None:
        sd_gate = pre_check_skin_delta_envelope(
            validated.plan.skin_delta, env.face_skin_max_delta,
            step_name="plan_validation",
        )
        if sd_gate.triggered:
            gate_state.results.append(sd_gate)
            validated.plan.skin_delta = sd_gate.adjusted_value
```

- [ ] **Step 3: Добавить вызов post_check_shadow_crush**

В `_record_step()` или после последнего шага (после shadow_floor), добавить:

```python
    # Gate: shadow_crush post-check
    if ctx.subject_mask is not None:
        sc_result = post_check_shadow_crush(
            shadow_crush_pct=crush_val,
            threshold_pct=10.0,
        )
        if sc_result.triggered:
            gate_state.results.append(sc_result)
```

Требуется знать точную структуру кода. Read `_record_step()` function and `_run_pipeline_steps()` around the post-checks (after shadow_floor).

- [ ] **Step 4: Запустить тесты**

```powershell
& ".venv\Scripts\python.exe" -m pytest tests/test_quality_gates.py -v --no-header
```

Expected: 20+ (all pass)

- [ ] **Step 5: Commit**

```powershell
git add retouch/processing/pipeline.py
git commit -m "fix: wire unused gates pre_check_skin_delta_envelope and post_check_shadow_crush

Both gates were imported but never called in _run_pipeline_steps().
Added calls in appropriate locations."
```

---

### Task C: Fix docs/reference/config.md — 7 outdated values for impact

**Problem:** Summary table (строки 33-57) и prose секция impact (строки 135-142) содержат неверные значения, не совпадающие с DEFAULTS/config.yaml.

**Files:**
- Modify: `docs/reference/config.md`

- [ ] **Step 1: Прочитать актуальные DEFAULTS**

```powershell
& ".venv\Scripts\python.exe" -c "
from retouch.config import DEFAULTS
d = DEFAULTS['processing']['impact']
print(f'stone_gamma: {d.get(\"stone_gamma\")}')
print(f'unsharp_threshold: {d.get(\"unsharp_threshold\")}')
print(f'shadow_floor: {d.get(\"shadow_floor\")}')
print(f'face_brightness_target_min: {d.get(\"face_brightness_target_min\")}')
print(f'face_brightness_target_max: {d.get(\"face_brightness_target_max\")}')
print(f'white_ceiling: {d.get(\"white_ceiling\")}')
print(f'glow_style: {d.get(\"glow_style\")}')
print(f'highlight_start: {DEFAULTS[\"processing\"][\"laser_standard\"].get(\"highlight_start\")}')
"
```

- [ ] **Step 2: Исправить summary table (строки 33-57)**

Read the table: найти каждое неверное значение и заменить.

| Параметр | Было (неверно) | Стало (верно) |
|----------|:--------------:|:-------------:|
| `stone_gamma` (impact) | 0.90 | 0.88 |
| `unsharp_threshold` (impact) | 2 | 1 |
| `shadow_floor` (impact) | 8 | 2 |
| `face_brightness_target_min` (impact) | 200 | 170 |
| `face_brightness_target_max` (impact) | 225 | 215 |
| `white_ceiling` (impact) | 240 | 245 |
| `glow_style` (impact) | outer | inner |
| `highlight_start` (laser_standard) | 210 | 200 |

- [ ] **Step 3: Исправить prose секцию impact (строки 135-142)**

Те же 7 параметров.

- [ ] **Step 4: Проверить нет ли других несоответствий**

```powershell
& ".venv\Scripts\python.exe" -c "
from retouch.config import DEFAULTS
for m in ['laser_standard', 'laser_80w', 'impact']:
    d = DEFAULTS['processing'][m]
    print(f'{m}: gamma={d.get(\"stone_gamma\")}, fb_min={d.get(\"face_brightness_target_min\")}, fb_max={d.get(\"face_brightness_target_max\")}, wc={d.get(\"white_ceiling\")}, sf={d.get(\"shadow_floor\")}, highlight_start={d.get(\"highlight_start\")}')
"
```

- [ ] **Step 5: Commit**

```powershell
git add docs/reference/config.md
git commit -m "docs: fix 7 outdated impact values in config.md

stone_gamma: 0.90→0.88, unsharp_threshold: 2→1,
shadow_floor: 8→2, fb_target_min: 200→170,
fb_target_max: 225→215, white_ceiling: 240→245,
glow_style: outer→inner, highlight_start(l80w): 210→200"
```

---

### Task D: Fix docs/reference/api.md — missing fields, wrong types

**Problem:**
1. PipelinePlan table — нет поля `profile`
2. StepMetricsRecord — поля `mean_by_zone`/`std_by_zone` не существуют, реальные: `zone_metrics`/`warnings`

**Files:**
- Modify: `docs/reference/api.md`

- [ ] **Step 1: Прочитать текущий api.md раздел PipelinePlan**

Read lines ~302-316.

- [ ] **Step 2: Добавить поле `profile` в таблицу PipelinePlan**

```markdown
| `profile` | str | Профиль обработки: `preserve`, `standard`, `diagnostic` |
```

- [ ] **Step 3: Исправить StepMetricsRecord поля**

Заменить:
```markdown
| `mean_by_zone` | dict[str, float] | Средняя яркость по каждой зоне |
| `std_by_zone` | dict[str, float] | Стандартное отклонение по каждой зоне |
```
на:
```markdown
| `zone_metrics` | dict[str, ZoneMetrics] | Метрики (median, p10, p90, p95, max, variance, clipped_pct) по каждой зоне |
| `warnings` | list[str] | Предупреждения quality gates для данного шага |
```

- [ ] **Step 4: Проверить что `profile` также есть в ValidatedPlan (если нужно)**

ValidatedPlan наследует PipelinePlan, поле `profile` уже в PipelinePlan.

- [ ] **Step 5: Commit**

```powershell
git add docs/reference/api.md
git commit -m "docs: fix api.md — add profile field, fix StepMetricsRecord fields"
```

---

### Task E: Fix docs/architecture/pipeline.md — wrong gate names, wrong profile names

**Problem:** 
1. Lines ~316-318: все 7 gate names неверные (например, `gate_chromakey_ratio` вместо `pre_check_face_dark_small`)
2. Line ~292: профили названы `soft`/`impact` вместо `preserve`/`standard`/`diagnostic`
3. Line ~320: GateResult поля `(pass/fail, severity, message)` неверные

**Files:**
- Modify: `docs/architecture/pipeline.md`

- [ ] **Step 1: Read current pipeline.md sections**

Read lines ~284-325.

- [ ] **Step 2: Исправить имена профилей**

Было: `standard (все шаги), soft (без face_correction), impact (с shadow_noise)`
Стало: `standard (все шаги), preserve (только chromakey → gray → glow → rolloff → vignette), diagnostic (с сохранением масок и метрик)`

- [ ] **Step 3: Исправить имена gates**

Было: `gate_chromakey_ratio`, `gate_face_detected`, `gate_analytics_valid`, `gate_clipping`, `gate_shadow_crush`, `gate_tonal_range`, `gate_face_brightness`
Стало: `pre_check_face_dark_small`, `pre_check_contour_inner_quality`, `pre_check_skin_delta_envelope`, `post_check_variance_loss`, `post_check_clipped_pct`, `post_check_p95_shift`, `post_check_shadow_crush`

- [ ] **Step 4: Исправить GateResult поля**

Было: `GateResult(pass/fail, severity, message)`
Стало: `GateResult(gate_name, step_name, triggered, original_value, adjusted_value, reason)`

- [ ] **Step 5: Commit**

```powershell
git add docs/architecture/pipeline.md
git commit -m "docs: fix pipeline.md — correct gate names and profile names"
```

---

### Task F: Fix docs/zones.md — non-existent rolloff_knee reference

**Problem:** Line ~116 ссылается на `config.yaml:rolloff_knee`, но такого параметра в config.yaml нет. Knee вычисляется в коде как `white_ceiling * 0.90`.

**Files:**
- Modify: `docs/zones.md`

- [ ] **Step 1: Read docs/zones.md Rolloff section**

Read lines ~103-128.

- [ ] **Step 2: Исправить ссылку**

Заменить:
```
`knee` — порог срабатывания (по умолч. 200, из `config.yaml:rolloff_knee`)
```
на:
```
`knee` — порог срабатывания (вычисляется как `white_ceiling × 0.90`, по умолч. 225 для laser_standard)
```

- [ ] **Step 3: Commit**

```powershell
git add docs/zones.md
git commit -m "docs: fix zones.md — remove reference to non-existent rolloff_knee config key"
```

---

### Task G: Verify full test suite

- [ ] **Step 1: Run full test suite**

```powershell
& ".venv\Scripts\python.exe" -m pytest tests/ --no-header 2>&1 | Select-String -Pattern "passed|failed|error" | Select-Object -Last 1
```

Expected: no new failures (same 19 pre-existing failures, 642+ passed).

---

## Итоговый чек-лист

- [ ] **A:** Critical bug — move stone_gamma/compression before enforcement block
- [ ] **B:** Wire unused gates (pre_check_skin_delta_envelope, post_check_shadow_crush)
- [ ] **C:** Fix 7 outdated values in docs/reference/config.md
- [ ] **D:** Fix api.md — add profile field, fix StepMetricsRecord fields
- [ ] **E:** Fix pipeline.md — correct gate names and profile names
- [ ] **F:** Fix zones.md — non-existent rolloff_knee reference
- [ ] **G:** Verify full test suite — no regressions
